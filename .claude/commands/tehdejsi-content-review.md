---
description: Audit and fix historical content, Czech certainty, sensitivity, source metadata, and sharing safety.
---

Use `$tehdejsi-svet-editorial-integrity`. Inspect source data, editorial rules, historical resolver, report composition, share selection, and relevant tests. For `cityImages`, also inspect the source page, current licence metadata and actual pixels; metadata alone cannot prove a suitable date, subject or crop. Verify external claims with primary or authoritative sources when web research is part of the request. Fix supported problems without inventing replacements. Run `npm run data:city-images` when its selection or generator changes, then `npm run data:public`, `npm run audit:content` and relevant tests; inspect generated output before committing.
