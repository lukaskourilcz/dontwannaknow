---
description: Run the authoritative Tehdejší svět release checks and report exact code, content, UX, asset, documentation, and Git status.
---

Use `$tehdejsi-svet-release-validation`. Inspect `package.json`, run `npm run check` and `npm audit --audit-level=moderate`, inspect production chunks, and complete critical browser flows. Search for old branding, unsupported public countries, placeholders, unsafe analytics, obvious type bypasses, duplicate primitives, unused media, and stale docs. Fix feasible failures, rerun the affected checks, inspect `git diff --check` and final status, and report exact results without claiming unexecuted checks.
